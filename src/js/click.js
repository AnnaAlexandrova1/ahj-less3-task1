/* export default function click() {
  const countClick = 0;
  const innerList = Array.from(document.getElementsByClassName('inner'));
  innerList.forEach((index) => {
    index.onclick = () => {
      if (index.hasChildNodes) {
        console.log('true');
      }
    };
  });
} */
